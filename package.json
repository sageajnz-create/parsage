{
  "name": "parsage",
  "version": "0.1.0",
  "description": "Plug-and-play low-latency game and desktop streaming for Linux and friends",
  "author": "Sage & Antigravity",
  "license": "MIT",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "build": "npm run build:web && cd server && npm run build",
    "build:web": "cd web && npm run build",
    "test": "cd server && npm test && cd ../web && npm run build && cd .. && node scripts/test-host.mjs",
    "test:native-browser": "npm run build && node scripts/test-native-browser.mjs",
    "test:native-portal": "npm run build && node scripts/test-native-browser.mjs --portal",
    "test:browser-reconnect": "npm run build && node scripts/test-browser-reconnect.mjs",
    "start": "node server/dist/index.js",
    "host": "python3 host/parsage_host.py"
  }
}
